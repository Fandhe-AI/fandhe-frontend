# NPM 静的アセット判定ルール設計（TASK-12.2a）

> **本書のステータスと前提**: 本書は TASK-12.2（親イシュー #121）の 4h
> 分割サブタスクのうち **設計**（TASK-12.2a・本イシュー #122）の成果物です。
> 兄弟サブタスクは TASK-12.2b（`check_static_only.py` 実装、#123）・
> TASK-12.2c（検証と CI 統合、#124）であり、判定スクリプト本体の実装・
> CI ワークフローへの組み込みはいずれも本書のスコープ外です
> （`docs/design/docker-wasm-build-stage.md`・`docs/design/dist-server-design.md` の
> 先行例と同型の設計契約ドキュメント）。
>
> TASK-12.2 は担当区分「共同」（設計判断を伴うタスク）に分類されています。
> 自動運転モードでの実装のため、判断が必要な境界ケースはすべて
> **安全側（fail-closed・既定拒否）** に倒して確定し、判断根拠を本書に
> 明記しました。人間レビュー（PR）で判断ポイントを確認し、緩和が必要な
> 場合は例外機構（§3.3）または後続 PR で個別に対応してください。

## 1. 目的とトレーサビリティ

- **関連要件**: REQ-12「NPM 互換（ビルド時静的アセット限定、実行時
  スコープ外）」の受け入れ基準 2「取り込んだパッケージが生成する成果物の
  うち、実行可能コード（`bin`/`main` 相当のエントリを持つ JS）を含まない
  ことを機械的に検証する仕組みを持つこと」
  （`docs/spec/04-requirements.md` 163 行目）。
- **背景**: PoC-6（`docs/spec/03-poc/npm-compat-feasibility/README.md`）は
  「`--ignore-scripts` はインストール時の暗黙実行（preinstall/install/
  postinstall）のみを防ぎ、パッケージ内の明示的な `require()`・ビルド
  プラグインとしての実行までは防げない」ことを実証しました。
  `tools/npm-asset-build/install.sh`（TASK-12.1a・#38、マージ済み）は
  この受け入れ基準 1 側（`--ignore-scripts` の迂回不能な強制）を担う入口
  ラッパーであり、本書はその**後段ゲート**として「取り込んだパッケージが
  実行可能コードを一切含まない静的アセットのみであること」を機械的に
  検証するルールを設計します。
- **親タスク**: TASK-12.2（#121、`docs/spec/05-tasks.md` 参照）。
- **サブタスク分割**:

| サブタスク | Issue | 内容 | 本書との関係 |
|-----------|-------|------|-------------|
| TASK-12.2a | #122（本書） | 静的アセット判定ルールの設計 | 本書そのもの |
| TASK-12.2b | #123 | `check_static_only.py` の実装 | 本書 §3・§4 が実装方針・CLI 契約を規定 |
| TASK-12.2c | #124 | 検証（fixture テスト）と CI 統合 | 本書 §3.4 のテスト設計方針・§5 の完了条件を引き継ぐ |

## 2. 判定対象と前提

- **判定対象**: `install.sh --dir <project-dir>` でインストールされた
  `<project-dir>/node_modules/` 配下の全パッケージ（scoped パッケージ
  `@scope/name` および transitive 依存を含む）。ここでの「全パッケージ」
  とは、ネストした `node_modules/`（npm が依存解決の都合で
  `<pkg>/node_modules/<dep>/` のように配置する transitive 依存）を含めて
  **各パッケージディレクトリを 1 単位として個別に判定する**ことを意味し、
  親パッケージが子パッケージ配下のファイルの責任を負う形にはしません
  （詳細は §3.2）。
- **実行タイミング**: インストール直後・生成物を配布物（`static/` 等）へ
  取り込む前。`install.sh` による `--ignore-scripts` 強制インストールの
  **後段ゲート**として位置づけ、両者は独立に迂回不能であることで多層防御
  を構成します（`install.sh` 単体では明示 `require()` は防げないため）。
- **前提契約**: 本検証はインストール経路が必ず `install.sh` 経由である
  ことを前提とします。`install.sh` を経由しない `node_modules` に対して
  実行しても構いませんが、その場合ライフサイクルスクリプトが実行済みの
  可能性がある点は本検証の範囲外です（`install.sh` の責務）。

## 3. 判定ルール（3 層検査）

判定は **denylist（既知の危険パターンを拒否）ではなく allowlist
（既知の安全パターンのみ許可・それ以外は既定拒否）** を基本方針とします。
根拠: 未知の実行形式（`.node`・`.wasm`・シェルスクリプト等）の取りこぼしを
構造的に防ぐためであり、`rws-core` の「既定エスケープ・明示オプトイン」と
同じ fail-safe 原則に従います。

### 3.1 Rule 1: `package.json` メタデータ検査

| 検査項目 | 判定 | 根拠 |
|---------|------|------|
| `bin` フィールドが存在（文字列・オブジェクトいずれも） | **違反** | REQ-12 受け入れ基準 2 の直接要求 |
| `main` / `module` / `browser` / `exports` が JS 実行エントリ（`.js`/`.mjs`/`.cjs`/`.node`）を指す | **違反** | 実行コードへの参照そのもの |
| `main` 等が非実行ファイル（例: `.css`）のみを指す | 許容 | CSS 配布パッケージの実在慣行を尊重（安全側判断だが過度な拒否は例外機構 §3.3 で救済する設計のため、ここは緩めてよい） |
| `main` 等のフィールドが欠落 | 違反としない | Rule 2 のファイル実体走査で担保するため、フィールド欠落のみを理由に拒否しない |
| `scripts` にライフサイクルスクリプト（`preinstall`/`install`/`postinstall`/`prepare` 等）が存在 | **違反** | `--ignore-scripts` で無効化済みだが、静的アセット限定パッケージとして本来不要であり、存在自体が「実行コードを想定したパッケージ」である兆候のため安全側で拒否する。過検知は例外機構で個別許可する |

### 3.2 Rule 2: ファイル実体の拡張子 allowlist 検査

パッケージ配下の全ファイルを走査し、**許可拡張子リストに一致しない
ファイルが 1 つでもあれば違反**とします。

**走査範囲の境界（ネストした `node_modules` の扱い）**: 走査は
パッケージディレクトリ配下のファイルのうち、**そのパッケージ自身が持つ
ネストした `node_modules/` ディレクトリの手前で止めます**。すなわち、
あるパッケージ `foo` の配下に `foo/node_modules/bar/` が存在する場合、
`foo` の判定走査は `foo/node_modules/` 自体を境界として除外し、
`bar` はこのパッケージ一覧走査（§2）が別途列挙する独立した判定対象と
して判定します。この境界がないと、素朴な再帰走査では `bar` 配下の
`.js` 等のファイルが `foo` の違反として検出されてしまい、`foo` 自体は
クリーンであるにもかかわらず transitive 依存の内容によって不合格に
なるという誤った責任分担が生じます。

**許可拡張子（初期案）**:

```
.css .woff .woff2 .ttf .otf .eot
.svg .png .jpg .jpeg .gif .webp .avif .ico
.json .md .txt
```

および拡張子なしファイルのうち `LICENSE` / `LICENSE-*` / `NOTICE` /
`README` / `README.*`（大文字小文字を区別しない前方一致）。

**明示的に拒否する境界ケース（判断根拠を明記）**:

| 拡張子・パターン | 判定 | 理由 |
|------|------|------|
| `.js` / `.mjs` / `.cjs` / `.node` / `.wasm`（`.min.js` を含む） | **拒否（例外機構でも免除不可・§3.4 参照）** | 実行可能コードそのもの。クライアント持ち込み JS は REQ-12 の除外事項 |
| `.sh` / `.py` / その他スクリプト言語拡張子 | **拒否** | 実行可能コード |
| `.d.ts`（TypeScript 型定義） | **拒否** | 実行コードではないが、本フレームワークは Rust 実装であり型定義の利用先がない。「使わないものは許可しない」という allowlist の既定拒否原則を優先する |
| `.map`（source map） | **拒否** | 対応する実行コード（JS）の存在を示唆し、静的アセット限定パッケージには本来不要 |
| `.scss` / `.less` | **拒否** | コンパイルにビルドプラグイン実行を要し、PoC-6 が「対象外」と結論づけた能動的コード実行の用途に接続するため |

### 3.3 Rule 3: ファイル属性・内容の追加検査

allowlist（Rule 2）を通過したファイルであっても、以下に該当すれば違反と
します（拡張子偽装・隠蔽への対策）。

- **shebang**: ファイル先頭が `#!` で始まる → **違反**。拡張子を偽装した
  実行スクリプトを検出するための保険。
- **実行ビット**: **通常ファイル（regular file）**に実行パーミッション
  （`+x`）が立っている → **違反**。静的アセットに実行ビットは本来不要。
  ディレクトリは走査対象の階層構造上 `+x`（実行 = トラバース権限）が
  立っているのが通常であり、本検査の対象外とします（ディレクトリを
  対象に含めると素朴な実装では全パッケージが誤検知で違反になるため）。
  symlink は本項の対象ではなく §4 の symlink 方針（走査除外・警告）に
  従います。
- **`.svg` 内のスクリプト混入**: `.svg` は許可拡張子だが、以下のいずれかに
  該当する場合は **違反**とします。
  - `<script` 要素
  - `on*` イベント属性（`onload=` 等）
  - `href` / `xlink:href` 属性値が `javascript:` または `data:` スキーム
    で始まる（`<a>` / `<use>` / `<image>` 等、SVG 内で `href` 系属性を
    持ちうる要素すべてが対象。`<foreignObject>` 要素自体の存在も同様に
    違反として扱う）

  SVG 経由のスクリプト混入・XSS 面（REQ-1 の既定エスケープ方針との整合）
  への防御であり、単純な文字列検査で安全側に倒します（正規表現による
  大雑把な一致で過検知を許容し、見逃しを避ける）。ただし本検査は
  既知の代表的な攻撃ベクタに対する広めの一致であり、SVG 仕様が持つ
  攻撃面（他の要素・属性の組み合わせ、パーサ差異等）を網羅的に塞ぐ
  ものではありません。§5「過大な安全性主張の禁止」と同じ立場で、
  本検査は防御の一層であって完全性を主張しないことを明記します。

## 3.4 例外・オプトイン機構

過検知（実在の静的アセットパッケージが誤って弾かれる）の救済として、
プロジェクト側の明示許可ファイル（例:
`tools/npm-asset-build/allowlist.toml`）で「パッケージ名 + 該当ルール ID +
（Rule 2 の場合は）対象拡張子またはファイルパス + 理由」を単位に個別免除
できる設計とします。

- `raw_html()` と同様に**明示オプトイン**の設計とし、パッケージ名の
  ワイルドカード指定・ルール全体の無効化（例: 「Rule 2 を全パッケージで
  無効化」）は設けません。免除は 1 パッケージ・1 ルール単位の宣言を
  基本としますが、**Rule 2 の免除は「パッケージ + ルール」単位では
  粒度が粗すぎるため、さらに「対象拡張子 or 個別ファイルパス」単位を
  必須とします**。理由: Rule 2 をパッケージ単位で免除すると、無害な
  追加ファイル（例: `.txt` の亜種）を許可する目的のはずが、同じ免除で
  そのパッケージ内の `.js` / `.mjs` / `.wasm` 等の実行コードに対する
  拒否まで無効化されてしまうため（Rule 3 は shebang と実行ビットのみを
  検査し、拡張子が `.js` 等である通常の JS ファイルはそもそも検出対象
  としていないため、Rule 2 の拒否をすり抜けた時点で機械的な歯止めが
  失われる）。
- **実行コード拡張子（`.js` / `.mjs` / `.cjs` / `.node` / `.wasm`）に対する
  Rule 2 の拒否は、いかなる粒度の例外エントリでも免除不可（ハード拒否）
  とします**。免除機構が救済できるのは実行コード以外の拡張子・ファイル
  （例: 未知の非実行ファイル形式）に限ります。これにより「Rule 2 免除で
  実行コードの拒否ごと解除される」抜け道を構造的に塞ぎます。
- 免除エントリには理由（自由記述）を必須項目とし、レビューで免除の妥当性
  を判断できるようにします。
- 例外機構のファイルフォーマット・パーサ実装は TASK-12.2b（#123）の
  スコープです。本書ではフォーマット方針と制約のみを確定します。

## 4. 実装インターフェース契約（#123 への引き渡し）

- **CLI**: `check_static_only.py --node-modules <path>`
  （エイリアスとして `--dir <project-dir>` から `<project-dir>/node_modules`
  を導出する形も許容してよい）。
- **終了コード**: `0` = 全パッケージ合格 / `1` = 違反あり / `2` = 実行
  エラー（パス不在等）。
- **出力形式**: 違反ごとに「パッケージ名 / ルール ID / 該当ファイルパス /
  理由」を 1 行で列挙する（CI ログで判読可能な形式。例:
  `VIOLATION package=foo rule=R1-bin file=package.json reason="bin field present"`）。
- **実装制約**: Python 3 標準ライブラリのみを使用する。pip 依存の追加は
  検証ツール自身のサプライチェーン面を拡大するため不可とします
  （依存クレート追加がユーザー承認必須であるのと同じ考え方を踏襲）。
- **symlink・パストラバーサル対策**: `node_modules` 配下の走査時に
  symlink を辿らない（symlink エントリはそれ自体を違反として扱うか、
  少なくとも走査対象から除外し警告を出す）。`node_modules` の外側へ
  出るパス（`..` を含む解決結果等）は走査対象に含めない。
- **テスト設計方針**: `test_install.sh`（TASK-12.1a の先例）に倣い、
  fixture パッケージ（合格 / `bin` あり / `.js` 混入 / shebang 混入 /
  実行ビット付き通常ファイル / 実行ビット付きディレクトリ（合格扱い
  であることの確認）/ SVG 内 `<script>` 混入 / SVG `href` の
  `javascript:` スキーム混入 / ネストした `node_modules` を持つ
  transitive 依存（親パッケージ自体はクリーンな場合に合格することの
  確認）/ Rule 2 の拡張子単位免除済み / 実行コード拡張子は免除エントリが
  あっても拒否されたままであることの確認）による
  オフラインテストを `tools/npm-asset-build/tests/` に配置する。

## 5. スコープ外の明記

- **CI ワークフローへの組み込み**: TASK-12.2c（#124）で
  `.github/workflows/ci.yml` の `npm-asset-build` ジョブとして統合済みです。
  `test_install.sh`（TASK-12.1a）は無条件（fail-closed）で実行し、
  `check_static_only.py`（TASK-12.2b・#123）本体は並列進行中のため、
  同ジョブ内は存在ガード（browser-test ジョブと同型）で判定します。
  未マージの間は該当ステップの fixture テストが `unittest.SkipTest` で
  全件スキップされてジョブは空実行相当で成功し、#123 マージ後に
  自動的に有効化されます。
- **配布バイナリ・ブラウザ配信物側の NPM 非混入検証**: TASK-12.3（#125）
  のスコープです。
- **過大な安全性主張の禁止**: 本検証はビルド時に取り込む静的アセットの
  実行コード非混入を機械的に検証するものであり、PoC-6 が指摘した
  「ビルド時のリスク（`build.rs`・手続きマクロ等、Cargo エコシステム
  全体の構造的リスク）」自体を解消するものではありません。本書・実装は
  この限界を明示し、「NPM パッケージを取り込んでも完全に安全」という
  一般化した主張は行いません。

## 6. セキュリティ考慮事項（OWASP Top 10 観点）

- **A03 インジェクション / XSS**: SVG 内 `<script>` 要素・イベント属性・
  `javascript:`/`data:` スキームの `href`/`xlink:href`（Rule 3）の検査
  により、静的アセット経由のスクリプト混入経路の代表的なベクタを狭めます。
  ただしこれは広めの一致による多層防御の一層であり、SVG 攻撃面全体を
  網羅的に塞ぐ主張はしません（§3.3・§5 参照）。既定エスケープ（REQ-1）を
  弱める記述は本書に含めていません。
- **A05 セキュリティ設定ミス**: 判定を denylist ではなく allowlist
  （既定拒否・fail-closed）で設計し、未知の実行形式の取りこぼしを
  構造的に防ぎます。例外機構は 1 パッケージ・1 ルール単位（Rule 2 は
  さらに対象拡張子・ファイルパス単位）の明示オプトインに限定し、
  ワイルドカード許可・ルール全体の無効化を禁止します。加えて、実行コード
  拡張子（`.js`/`.mjs`/`.cjs`/`.node`/`.wasm`）に対する拒否はいかなる
  例外エントリでも免除不可のハード拒否とし、「パッケージ単位の免除が
  実行コード拒否ごと無効化する」抜け道を構造的に防ぎます（§3.4）。
- **A06 脆弱な依存 / A08 サプライチェーン**: 本タスク自体が REQ-12 の
  サプライチェーン対策（PoC-2 脅威モデル面 (b)）の中核です。
  `check_static_only.py` を Python 標準ライブラリのみで実装する制約を
  課し、検証ツール自身が新たな依存面を持ち込まないようにします。
  `--ignore-scripts` の限界（明示実行までは防げない）を本書で明記し、
  過大な安全性主張を避けています（§5）。
- **A01 アクセス制御 / パストラバーサル**: `node_modules` 走査時の
  symlink 追跡禁止・走査範囲外パスの除外方針を §4 に明記しました。
- **機微情報の非混入**: 本書・fixture 仕様に実クレデンシャル・実在
  パッケージの脆弱性詳細は含めていません。

## 7. リスクと安全側の判断（担当「共同」タスクとしての明記）

本タスクは担当区分「共同」であり、本来は人間の方針決定を伴います。
自動運転モードでの実装のため、以下の判断ポイントはすべて**拒否側
（安全側）に倒して確定**しました。人間レビュー（PR）で妥当性を確認して
ください。

1. **Rule 1 の `scripts` フィールド違反扱い**（§3.1）: ライフサイクル
   スクリプトの存在自体を違反とする判断。過検知の可能性あり。
2. **`.d.ts` / `.map` / `.scss` / `.less` の拒否**（§3.2）: 実行コードでは
   ないが、用途がない・実行コードへの接続を示唆するという理由で拒否。
3. **allowlist 方式の採用**（§3 冒頭）: denylist より保守コストが高いが、
   未知の実行形式を構造的に防げる安全側の設計。

緩和方向（許可の追加）は例外機構（§3.3）または後続 PR で対応可能ですが、
逆方向（拒否の追加）は既存の取り込み済みパッケージに対して破壊的になり
うるため、初期設計は厳格側を正としています。
