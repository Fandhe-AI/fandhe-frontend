[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "xtask: CI 計測用ツール（依存グラフ件数・深さ計測 等）。外部依存ゼロ。"
publish = false

# REQ-3 / coding-rust.md: 依存クレートの追加は事前に cargo metadata で影響を確認し
# ユーザー承認を得ること。xtask 自身も依存グラフ上限の計測対象になり得るため、
# 計測ツール自体が依存を増やして基準をぼかすことのないよう外部依存ゼロを維持する。
[dependencies]

[dev-dependencies]
