[package]
name = "fandhe-frontend-core"
version = "0.4.0"
edition = "2021"
description = "fandhe-frontend-core: 描画コア（ノード木 API・既定エスケープレンダラ）。外部依存ゼロ。"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Fandhe-AI/fandhe-frontend"
readme = "../../README.md"
keywords = ["frontend", "html", "ssr", "escaping", "security"]
categories = ["web-programming", "template-engine"]

# REQ-3 / coding-rust.md: core は外部依存ゼロを厳守する。
# 新規クレートを追加する場合は事前にユーザー承認を得ること。
[dependencies]

[dev-dependencies]
