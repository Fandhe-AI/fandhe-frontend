# Load repository root `.env` into the shell session via direnv.
# `dotenv_if_exists` は direnv 組み込みで、dotenv 形式をパースして変数を export する。
# 用途: shell から参照するツール用 env (`.mcp.json` から `${CHAKRA_PRO_API_KEY}` で
# 展開される値など)。
dotenv_if_exists .env
